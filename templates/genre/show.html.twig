{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_index') }}" class="btn btn-dark mt-1 mb-1">back to list</a>

    <a href="{{ path('genre_edit', {'id': genre.id}) }}" class="btn btn-success mt-1 mb-1">edit</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
